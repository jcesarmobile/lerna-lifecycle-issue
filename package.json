{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "release": "lerna publish --yes --loglevel silly --skip-npm",
    "version": "echo \"version!\"",
    "postversion": "echo \"postversion!\" && exit 1"
  },
  "devDependencies": {
    "lerna": "^3.0.0-alpha.0"
  }
}
